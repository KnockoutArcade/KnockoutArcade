/// @description Insert description here
// You can write your code in this editor
/*
var isRoundStart = false;

if (sprite_index == sRound1Start || sprite_index == sRound2Start || sprite_index == sFinalRoundStart)
{
	isRoundStart = true;
}

draw_text(x, 0, startDelay);
draw_text(x, 10, visible);
draw_text(x, 20, isRoundStart);
draw_text(x, 30, global.campaignStartLevelDelay);
draw_text(x, y, x);
draw_text(x+ 30, y, y);

*/
draw_self();